<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Image - The Gleary</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/newStyle.css">
</head>
<body>
  <div class="container col-lg-6 mt-5 p-4 shadow rounded bg-light">
    <h2 class="text-center text-warning mb-4 fw-bold">Edit Image</h2>
    <form method="POST" action="/gleary/<%= data._id %>?_method=PUT">
      <div class="form-floating mb-3">
        <input type="text" name="title" class="form-control" id="title" placeholder="Title" value="<%= data.title %>" required>
        <label for="title">Title</label>
      </div>

      <div class="form-floating mb-3">
        <input type="text" name="image" class="form-control" id="image" placeholder="Image URL" value="<%= data.image %>" required>
        <label for="image">Image URL</label>
      </div>

      <div class="form-floating mb-3">
        <textarea name="des" class="form-control" id="des" style="height: 120px;" required><%= data.des %></textarea>
        <label for="des">Description</label>
      </div>

      <div class="d-flex justify-content-between">
        <a href="/gleary/<%= data._id %>" class="btn btn-outline-secondary">Cancel</a>
        <button class="btn btn-warning">Update</button>
      </div>
    </form>
  </div>
</body>
</html>
