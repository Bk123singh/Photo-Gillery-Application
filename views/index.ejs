<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Gleary - All Images</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/gleary">The Gleary</a>
      <a href="/gleary/new" class="btn btn-light btn-sm">➕ Add Image</a>
    </div>
  </nav>

  <div class="container my-5">
    <h2 class="text-center mb-4 fw-semibold text-dark">All Image Collections</h2>
    <div class="row g-4">
      <% datas.forEach(data => { %>
        <div class="col-md-4">
          <div class="card h-100 shadow-sm border-0 hover-card">
            <img src="<%= data.image %>" class="card-img-top rounded-top" alt="Image" height="250" style="object-fit: cover;">
            <div class="card-body">
              <h5 class="card-title fw-bold"><%= data.name %></h5>
              <p class="card-text text-secondary"><%= data.des.substring(0, 80) %>...</p>
              <p class="text-muted small mb-3">
                📅 <%= data.Date ? data.Date.toString().split(' ').slice(0, 5).join(' ') : 'No date' %>
              </p>
              <div class="d-flex justify-content-between">
                <a href="/gleary/<%= data._id %>" class="btn btn-outline-primary btn-sm">View</a>
                <a href="/gleary/<%= data._id %>/edit" class="btn btn-outline-warning btn-sm">Edit</a>
                <form method="POST" action="/gleary/<%= data._id %>?_method=DELETE" class="d-inline">
                  <button class="btn btn-outline-danger btn-sm">Delete</button>
                </form>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <footer class="text-center text-muted py-3 border-top">
    © 2025 The Gleary | Made by Bishal Kumar
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
